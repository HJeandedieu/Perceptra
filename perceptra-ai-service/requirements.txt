# Perceptra AI Engine — Production Dependencies

# API server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Computer vision & inference
ultralytics==8.2.18
opencv-python-headless==4.9.0.80

# HTTP client (publisher)
httpx==0.27.0

# WebSocket client (publisher)
websockets==12.0

# Numerical computing
numpy>=2.0.0

# PyTorch — pin to 2.5.1 to avoid weights_only breaking change in 2.6
torch==2.5.1
torchvision==0.20.1

# Environment variables
python-dotenv==1.0.1